Design a high-fidelity AI Grounds interactive learning playground page for Layer Normalization Lab. Match the AI Grounds cross entropy reference exactly in structure and tone: bright white/cool-gray background, huge black page title at upper left, short dark-blue subtitle underneath, one pale outlined help button at upper right, no logo, no top-left brand mark, no global navigation, no sidebar. Content is a centered dense lesson page, not a landing page. Use numbered compact lesson panels with pale blue borders, white panels, electric indigo section titles and active controls, mono numeric/formula details, and no decorative chrome, no nested decorative cards, no ornamental backgrounds.

Teach one core intuition: LayerNorm normalizes across the hidden features inside one token row. Other token rows do not contribute to that token's mean or variance. The learner selects one token row and adjusts feature values; the chosen row updates the formula, normalized bars, gamma/beta output, and axis comparison.

Use deliberately simple internally consistent numbers in the mockup. For the selected token "cat", show raw hidden vector x = [-1.00, -1.00, 1.00, 1.00]. Show mean mu = 0.0000, variance sigma^2 = 1.0000, standard deviation sigma = 1.0000, and normalized x-hat = [-1.00, -1.00, 1.00, 1.00]. In the raw and normalized charts, bars should match those values exactly and be centered around a visible zero line. Show "mean of x-hat ≈ 0.0000" and "variance of x-hat ≈ 1.0000". For learned parameters use gamma = [1.20, 0.80, 1.00, 1.10] and beta = [0.10, -0.20, 0.00, 0.30], so output y = [-1.10, -1.00, 1.00, 1.40]. Display those output bar values consistently. Do not display a numeric mean or variance for y after scale and shift; instead show a compact note: "gamma/beta may change scale again".

Panel layout:
1. CHOOSE THE TOKEN & HIDDEN FEATURES: three token pills "The", "cat", "sat"; selected cat row highlighted. Show a compact 3x4 hidden-feature grid where cat row is active and other rows are faint. Show four slider-like controls for x1..x4 using the exact cat values above.
2. WATCH THE FORMULA NORMALIZE: formula stack mu = 1/d sum x_i, variance = 1/d sum (x_i - mu)^2, x-hat_i = (x_i - mu) / sqrt(variance + epsilon). Put exact numeric results beside the formulas. Beside it, show raw feature bars and normalized z-score bars. The green check panel should only show mean of x-hat approximately 0 and variance of x-hat approximately 1.
3. SCALE AND SHIFT: gamma and beta controls plus output chart y_i = gamma_i * x-hat_i + beta_i with exact y values [-1.10, -1.00, 1.00, 1.40]. Include the note "gamma/beta may change scale again" and no post-scale variance value.
4. COMPARE THE AXIS: small two-part grid: LayerNorm highlights one horizontal token row across features; BatchNorm highlights one vertical feature column across tokens. Text: "LayerNorm: across features in this token" and "BatchNorm: down a feature across the batch".
5. TAKEAWAY: concise note: "Each token gets its own stats; scale is stabilized before learned gamma/beta restore useful feature sizes."

Keep the design dense and readable. Avoid marketing hero layout, decorative orbs, excessive whitespace, malformed formulas, impossible numbers, clipped labels, chart overflow, hidden marks, duplicated axis ticks, or text crowding.
