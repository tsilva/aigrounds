Regenerate the final high-fidelity AI Grounds interactive learning playground page for Zero-Knowledge Proofs, preserving the correct graph, math, secrecy model, and chart from the previous pass. Fix only the slider and minor copy integrity issues: the rounds slider must NOT show dense tick labels. It should show only left endpoint "1", right endpoint "20", and the selected thumb label "6" with text "Current k = 6". No repeated tick labels, no malformed numbers, no clipped text.

Style: match AI Grounds cross-entropy reference: no logo, no brand mark, no global nav, no sidebar; huge black page title upper left "Zero-Knowledge Proofs"; short dark-blue subtitle; one pale outlined help button upper right; centered dense lesson page; compact numbered panels with pale blue borders, white surfaces, subtle cool shadows, electric indigo section titles, mono formula/numeric details. Desktop app screenshot, 1440px wide.

Core protocol: graph-coloring zero-knowledge proof. The prover knows a valid 3-coloring. Before each round, the prover secretly permutes color names and commits to all node colors. The verifier challenges one random edge. Only the two endpoint commitments are opened. If endpoint colors differ, the verifier gains evidence that the prover knows a valid coloring. The hidden fresh permutation prevents transcripts from accumulating into the original secret coloring.

Graph constraints: Draw an 8-node graph labeled A, B, C, D, E, F, G, H with EXACTLY these 9 edges and no other edges: A-B, B-C, C-D, D-E, E-F, F-G, G-H, H-A, A-E. Title: "Graph (8 nodes, 9 edges)". Highlight challenged edge A-B in indigo. Show only A and B opened with different color chips red and blue. All other nodes remain closed envelope/commitment tiles. Do not reveal the full coloring. Do not draw an F-B diagonal or any tenth edge.

Panel 1 title: "1. Choose The Prover". Segmented controls Honest prover selected and Cheating prover unselected. Fact pills: "Secret = valid 3-coloring"; "Fresh shuffle = hidden every round"; "Reveal = one edge only"; "Verifier learns = endpoint colors differ"; "Full coloring = hidden".

Panel 2 title: "2. Commit, Shuffle, Open One Edge". Left: graph as specified, with A-B opened red/blue and green PASS badge. Compact pills: "Fresh hidden shuffle: s6" and "Only this edge opened." Right: transcript table title "Transcript (verifier view)" with columns Round, Challenge edge, Opened colors, Shuffle token, Verdict. Rows: 1 A-B red/blue s1 PASS; 2 D-E blue/green s2 PASS; 3 C-D green/red s3 PASS. Rounds slider set to 6, showing only endpoints 1 and 20 plus the selected thumb label 6. Next challenge button. Helper line: "Each click runs the next round with a new random edge and fresh shuffle."

Panel 3 title: "3. Confidence Grows, Secret Stays Hidden". Formula block: Honest prover pass probability = 1.00. Cheater escape after k rounds = (passing edges / total edges)^k = (7 / 9)^6 = 0.22. Legend: "Passing edges (different colors) = 7"; "Caught edges (same color) = 2"; "passing edges + caught edges = 9". Chart must use x-axis ticks 0, 2, 4, 6, 8, 10 only and values 1.00, 0.60, 0.37, 0.22, 0.13, 0.08. The highlighted vertical line must be at x=6 and intersect the point labeled "k = 6, 0.22".

Secrecy meter says "Opened each round = 1 edge" and "Shuffle token changes each round". Callout: "Secret coloring remains hidden. Single-edge openings plus fresh shuffles cannot reveal the full coloring." Final takeaway exactly: "Takeaway: Each round proves one local check; the hidden shuffle keeps those checks from revealing the whole coloring."

Design quality constraints: compact numbered panels, no nested decorative cards, no ornamental backgrounds, no marketing hero layout, no clipped text, no duplicated axis ticks, no crowded labels, no inconsistent probabilities, no typos, no malformed words, no full-coloring reveal beyond the challenged edge. Every control visibly affects at least two teaching surfaces: graph/transcript and formula/risk meter.
